<div class="icon" style="width:{{ 100/count($data['branches']) }}%">
    <div class="big-icon"><a href="#{{ $href }}" data-scroll="{{ $href }}"><img src="{{ asset($icon) }}" /></a></div>
    <div class="big-icon-subscribe">{{ $subscribe }}</div>
</div>